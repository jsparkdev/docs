---
title: fetch
---

- 페이지 대신 데이터만 요청하기 위해 `fetch` 메서드 사용
- 이 메서드는 요청 객체를 서버로 전달, 서버는 응답 객체를 반환
- 브라우저나 런타임은 받은 응답을 파싱하여 데이터를 스크립트에서 사용
- 파싱으로 얻은 데이터는 일반적으로 JSON이며, 이를 위한 `json` 메서드가 내장되어 있음
- 요청 시 헤더를 설정할 수 있음. 헤더는 데이터를 위해 사용할 수 있는 메타데이터를 포함.
